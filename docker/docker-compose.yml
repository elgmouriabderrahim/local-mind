services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        UID: 1000
        GID: 1000
    container_name: laravel_app
    working_dir: /var/www
    volumes:
      - ../src:/var/www
    depends_on:
      - db
    networks:
      - laravel

  web:
    image: nginx:alpine
    container_name: laravel_nginx
    ports:
      - "8000:80"
    volumes:
      - ../src:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - laravel

  db:
    image: postgres:17
    container_name: laravel_db
    environment:
      POSTGRES_DB: local_mind
      POSTGRES_USER: elgmouri
      POSTGRES_PASSWORD: asareicola2233
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    networks:
      - laravel

  pgadmin:
    image: dpage/pgadmin4
    container_name: laravel_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: asareicola2233
    ports:
      - "5051:80"
    depends_on:
      - db
    networks:
      - laravel

networks:
  laravel:

volumes:
  pgdata:
